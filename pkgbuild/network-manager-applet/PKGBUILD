pkgname=network-manager-applet
pkgver=1.8.14
pkgrel=1
pkgdesc="Applet for managing network connections"
arch=('x86_64')
url="https://wiki.gnome.org/Projects/NetworkManager/"
license=('GPL2' 'LGPL2.1')
#depends=()
source=("https://download.gnome.org/sources/network-manager-applet/${pkgver:0:3}/network-manager-applet-${pkgver}.tar.xz")

build() {
	cd "${srcdir}/network-manager-applet-${pkgver}"

	./configure --prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--without-selinux

	make
}

package() {
	cd "${srcdir}/network-manager-applet-${pkgver}"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('e446a2e90faf17e672fa79a53280957dfd9a7a022293dfd578b4ebb71e821c2f387f8ec821c2dcd9a24d1e00e0bd32a4ac3c5d802b9a4fdd7f876c870f406eea')
