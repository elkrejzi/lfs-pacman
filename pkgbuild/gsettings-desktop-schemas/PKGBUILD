pkgname=gsettings-desktop-schemas
pkgver=3.24.0
pkgrel=1
pkgdesc="Shared GSettings schemas for the desktop"
arch=('x86_64')
url="https://www.gnome.org/"
license=(GPL)
#depends=()
source=("https://download.gnome.org/sources/gsettings-desktop-schemas/${pkgver:0:4}/gsettings-desktop-schemas-${pkgver}.tar.xz")

build() {
	cd "${srcdir}/gsettings-desktop-schemas-${pkgver}"

	./configure --prefix=/usr --disable-schemas-compile

	make
}

package() {
	cd "${srcdir}/gsettings-desktop-schemas-${pkgver}"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('b19fb29da29a3fa114536c0ce94b160f9c589568b626e487b5eca008791e539de9a89a81c69e41f8070b0369e6cfc800fe8b70247ecbab1bc32f24db759037c1')
