pkgname=libblockdev
pkgver=2.18
pkgrel=1
pkgdesc="A library for manipulating block devices"
arch=('x86_64')
url="https://github.com/storaged-project/libblockdev"
license=('LGPL')
#depends=()
source=("https://github.com/storaged-project/libblockdev/releases/download/${pkgver}-1/libblockdev-${pkgver}.tar.gz")

prepare(){
	cd "${srcdir}/libblockdev-${pkgver}"

	sed -i "s# -Werror##" src/utils/Makefile.in src/plugins/Makefile.in src/plugins/fs/Makefile.in
}

build() {
	cd "${srcdir}/libblockdev-${pkgver}"

	./configure --prefix=/usr \
		--sysconfdir=/etc \
		--without-nvdimm

	make
}

package() {
	cd "${srcdir}/libblockdev-${pkgver}"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('65e2f795ced3e902af889f3bd9e303234e1611fb25555123b4edaf0b1e3aca24d399422f96eb0694ada025e57ddeb207f60c8666d0640110e228cfb8da92d5ab')
