pkgbase=libxml2
pkgname=('libxml2' 'lib32-libxml2')
pkgver=2.9.6
pkgrel=1
arch=('x86_64')
url="http://www.xmlsoft.org/"
license=('MIT')
#depends=()
source=("http://xmlsoft.org/sources/libxml2-${pkgver}.tar.gz"
	"libxml2-remove-pyverify_fd.patch")

prepare() {
	cd "${srcdir}/libxml2-${pkgver}"

	patch -Np1 -i "${srcdir}/libxml2-remove-pyverify_fd.patch"

	cp -a "${srcdir}"/libxml2{,32}-${pkgver}
}

build() {
	cd "${srcdir}/libxml2-${pkgver}"

	./configure --prefix=/usr \
		--with-history \
		--with-icu \
		--with-python=/usr/bin/python2

	cd "${srcdir}/libxml232-${pkgver}"

	export CC_HOLD="${CC}" CXX_HOLD="${CXX}"
	export CC="${CC} -m32" CXX="${CXX} -m32"
	export PKG_CONFIG_PATH=/usr/lib32/pkgconfig

	./configure --prefix=/usr \
		--libdir=/usr/lib32 \
		--with-history \
		--with-icu \
		--without-python

	export CC="${CC_HOLD}" CXX="${CXX_HOLD}"
	unset CC_HOLD CXX_HOLD PKG_CONFIG_PATH

	cd "${srcdir}"

	make -C "${srcdir}/libxml2-${pkgver}"
	make -C "${srcdir}/libxml232-${pkgver}"
}

package_libxml2() {
	pkgdesc="XML parsing library, version 2"

	cd "${srcdir}/libxml2-${pkgver}"

	make install DESTDIR="${pkgdir}"
}

package_lib32-libxml2() {
	pkgdesc="XML parsing library, version 2 (32 bit)"

	cd "${srcdir}/libxml232-${pkgver}"

	make install DESTDIR="${PWD}/dest"

	install -v -dm755 "${pkgdir}/usr"
	mv dest/usr/lib32 "${pkgdir}/usr"
}

sha512sums=('5ef80f895374bd5dd3bcd5f00c715795f026bf45d998f8f762c0cdb739b8755e01de40cf853d98a3826eacef95c4adebe4777db11020e8d98d0bda921f55a0ed'
            '05c909c890ccc8db6cfc8890250085e2ff01c2e6c80995af1bd0a642f5665731188ffe9eeac4f2e28bce281cbd8fe28ef6a9bf106ea982a785da9dca548a65c9')
