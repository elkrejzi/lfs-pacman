pkgname=fribidi
pkgver=1.0.4
pkgrel=1
pkgdesc="A Free Implementation of the Unicode Bidirectional Algorithm"
arch=('x86_64')
url="https://github.com/fribidi/fribidi/"
license=('LGPL')
#depends=()
source=("https://github.com/fribidi/fribidi/releases/download/v${pkgver}/fribidi-${pkgver}.tar.bz2")

build() {
	cd "${srcdir}/fribidi-${pkgver}"

	./configure --prefix=/usr --disable-docs

	make
}

package() {
	cd "${srcdir}/fribidi-${pkgver}"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('435bd48c48ec3c2dc1f1266e401abdbd400d2202b55df6367dbe84580c21e4aa73205058324cf630dfec365733d3e0ac186d14608baa1c9f63231875fb7cc525')
