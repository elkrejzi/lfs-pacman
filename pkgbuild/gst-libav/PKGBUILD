pkgname=gst-libav
pkgver=1.12.0
pkgrel=1
pkgdesc="GStreamer Multimedia Framework ffmpeg Plugins"
arch=('x86_64')
url="https://gstreamer.freedesktop.org/"
license=('GPL')
#depends=()
source=("https://gstreamer.freedesktop.org/src/gst-libav/gst-libav-${pkgver}.tar.xz")

build() {
	cd "${srcdir}/gst-libav-${pkgver}"

	./configure --prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--libexecdir=/usr/lib \
		--with-system-libav \
		--with-package-name="GStreamer ffmpeg Plugins ${pkgver} Krejzi" \
		--with-package-origin="https://github.com/elkrejzi/"

	make
}

package() {
	cd "${srcdir}/gst-libav-${pkgver}"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('16c8862e5b2305215f01ac0c1514aa48d11359a1a06e6edbf7b9da60ab90bd8a154ddbb84b70fb05fde1eae503a91a18226f4d70c713f428334a7a8eb580a4c7')
