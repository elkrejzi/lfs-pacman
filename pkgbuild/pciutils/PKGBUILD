pkgname=pciutils
pkgver=3.6.0
pkgrel=1
pkgdesc="PCI bus configuration space access library and tools"
arch=('x86_64')
url="http://mj.ucw.cz/sw/pciutils/"
license=('GPL2')
#depends=()
source=("https://www.kernel.org/pub/software/utils/pciutils/pciutils-${pkgver}.tar.xz")

prepare() {
	cd "${srcdir}/pciutils-${pkgver}"

	wget http://pci-ids.ucw.cz/v2.2/pci.ids -O pci.ids
}

build() {
	cd "${srcdir}/pciutils-${pkgver}"

  	sed -i "s#gcc#${CC}#g" Makefile

	make PREFIX=/usr \
		SHAREDIR=/usr/share/misc \
		SHARED=yes ZLIB=no \
		OPT="${CFLAGS}"
}

package() {
	cd "${srcdir}/pciutils-${pkgver}"

	make PREFIX=/usr \
		SHAREDIR=/usr/share/misc \
		SHARED=yes ZLIB=no \
		OPT="${CFLAGS}" install \
		install-lib DESTDIR="${pkgdir}"

	chmod 755 "${pkgdir}/usr/lib/libpci.so"

	install -v -dm755 "${pkgdir}/usr/bin"
	mv -v "${pkgdir}/usr/sbin/lspci" "${pkgdir}/usr/bin"
}

sha512sums=('c86e7640ce488aa72404ec5718d6cd08c59c7f6e704b8a809e5a836760935e8a860ee9a54f0a725aa181f3cd71752b7bdd48db211f2574c9df12da1bf1310e49')
