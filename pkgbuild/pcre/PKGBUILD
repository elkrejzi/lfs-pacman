pkgname=pcre
pkgver=8.40
pkgrel=1
pkgdesc="A library that implements Perl 5-style regular expressions"
arch=('x86_64')
url="http://www.pcre.org/"
license=('BSD')
groups=('core')
#depends=()
source=("https://ftp.csx.cam.ac.uk/pub/software/programming/pcre/pcre-${pkgver}.tar.bz2")

build() {
	cd "${srcdir}/pcre-${pkgver}"

	./configure --prefix=/usr \
		--enable-unicode-properties \
		--enable-pcre16 \
		--enable-pcre32 \
		--enable-jit \
		--enable-pcregrep-libz \
		--enable-pcregrep-libbz2 \
		--enable-pcretest-libreadline

	make
}

package() {
	cd "${srcdir}/pcre-${pkgver}"

	make install DESTDIR="${pkgdir}"

	install -v -dm755 "${pkgdir}/lib"

	mv -v "${pkgdir}"/usr/lib/libpcre.so.* "${pkgdir}/lib"
	ln -sfv ../../lib/$(readlink "${pkgdir}/usr/lib/libpcre.so") "${pkgdir}/usr/lib/libpcre.so"
}

md5sums=('41a842bf7dcecd6634219336e2167d1d')
