pkgname=mpfr
_basever=3.1.6
pkgver=3.1.6
pkgrel=1
pkgdesc="Multiple-precision floating-point library"
arch=('x86_64')
url="http://www.mpfr.org/"
license=('LGPL')
#depends=()
source=("http://www.mpfr.org/mpfr-current/mpfr-${_basever}.tar.xz")
#	"mpfr-cumulative.patch::http://www.mpfr.org/mpfr-current/allpatches")

prepare() {
	cd "${srcdir}/mpfr-${_basever}"

	[ ${_basever} != ${pkgver} ] && patch -Np1 -i "${srcdir}/mpfr-cumulative.patch" || true
}

build() {
	cd "${srcdir}/mpfr-${_basever}"

	./configure --prefix=/usr --enable-thread-safe

	make
}

package() {
	cd "${srcdir}/mpfr-${_basever}"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('746ee74d5026f267f74ab352d850ed30ff627d530aa840c71b24793e44875f8503946bd7399905dea2b2dd5744326254d7889337fe94cfe58d03c4066e9d8054')
