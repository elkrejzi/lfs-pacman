pkgname=kdelibs4support
pkgver=5.35.0
pkgrel=1
pkgdesc="Porting aid from KDELibs4"
arch=('x86_64')
url="https://community.kde.org/Frameworks"
license=('LGPL')
#depends=()
options=(docs)
source=("https://download.kde.org/stable/frameworks/${pkgver:0:4}/portingAids/kdelibs4support-${pkgver}.tar.xz")

prepare() {
	install -v -dm755 "${srcdir}/build"
}

build() {
	cd "${srcdir}/build"

	cmake -DCMAKE_INSTALL_PREFIX=/usr \
		-DKDE_INSTALL_SYSCONFDIR=/etc \
		-DCMAKE_BUILD_TYPE=Release \
		-DKDE_INSTALL_LIBDIR=lib \
		-DBUILD_TESTING=OFF \
		"${srcdir}/kdelibs4support-${pkgver}"

	make
}

package() {
	cd "${srcdir}/build"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('94fd4c38312659e6286e9c7c8dcda4850cc8e4617dd1ce7718f4f1e5a845a0ce017fe40443cf325a509a7cddfb67d52f5b0c66b51a0a78d77ab1d6b087279e1e')
