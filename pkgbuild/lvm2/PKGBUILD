pkgname=lvm2
pkgver=2.02.176
pkgrel=1
pkgdesc="Logical Volume Manager 2"
arch=('x86_64')
url="http://sourceware.org/lvm2/"
license=('GPL2' 'LGPL2.1')
#depends=()
source=("ftp://sourceware.org/pub/lvm2/releases/LVM2.${pkgver}.tgz")

prepare() {
	cd "${srcdir}/LVM2.${pkgver}"

	sed -i "s#systemd >=#lib&#g" configure
}

build() {
	cd "${srcdir}/LVM2.${pkgver}"

	./configure --prefix=/usr \
		--with-confdir=/etc \
		--enable-applib \
		--enable-cmdlib \
		--enable-dmeventd \
		--enable-pkgconfig \
		--enable-udev_rules \
		--enable-udev_sync \
		--enable-udev-systemd-background-jobs \
		--with-tmpfilesdir=/usr/lib/tmpfiles.d \
		--with-systemdsystemunitdir=/usr/lib/systemd/system \
		systemdutildir=/usr/lib/systemd

	make
}

package() {
	cd "${srcdir}/LVM2.${pkgver}"

	make install DESTDIR="${pkgdir}"
	make install_systemd_units DESTDIR="${pkgdir}"
	make install_systemd_generators DESTDIR="${pkgdir}"
}

sha512sums=('eee0b1cc356f0c5ff5dba8efe81af5f0ad446649dc237458bb63419483899becc4fe6912592555f84cf9cb8f9cbdec341d2047db5943ca9a3da5f0af60b70759')
