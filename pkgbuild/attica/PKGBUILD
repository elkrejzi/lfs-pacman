pkgname=attica
pkgver=5.35.0
pkgrel=1
pkgdesc="Qt5 library that implements the Open Collaboration Services API"
arch=('x86_64')
url="https://community.kde.org/Frameworks"
license=('LGPL')
#depends=()
options=(docs)
source=("https://download.kde.org/stable/frameworks/${pkgver:0:4}/attica-${pkgver}.tar.xz")

prepare() {
	install -v -dm755 "${srcdir}/build"
}

build() {
	cd "${srcdir}/build"

	cmake -DCMAKE_INSTALL_PREFIX=/usr \
		-DKDE_INSTALL_SYSCONFDIR=/etc \
		-DCMAKE_BUILD_TYPE=Release \
		-DKDE_INSTALL_LIBDIR=lib \
		-DBUILD_TESTING=OFF \
		"${srcdir}/attica-${pkgver}"

	make
}

package() {
	cd "${srcdir}/build"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('704c2a6d2dc1f8b4a4ab67e9bc0bd9d7eb1d941c5d65c0709b77c61415df0b1b1b11fd6c364822283d6ecfb26da22588e63d0438e5885aefe2b6046478cd2600')
