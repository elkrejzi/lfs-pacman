pkgname=gnutls
pkgver=3.5.13
pkgrel=1
pkgdesc="A library which provides a secure layer over a reliable transport layer"
arch=('x86_64')
url="http://www.gnutls.org/"
license=('GPL3' 'LGPL2.1')
#depends=()
source=("ftp://ftp.gnutls.org/gcrypt/gnutls/v${pkgver:0:3}/gnutls-${pkgver}.tar.xz")

build() {
	cd "${srcdir}/gnutls-${pkgver}"

	./configure --prefix=/usr \
		--enable-local-libopts \
		--disable-guile

	make
}

package() {
	cd "${srcdir}/gnutls-${pkgver}"

	make install DESTDIR="${pkgdir}"

	make -C doc/reference install-data-local DESTDIR="${pkgdir}"

	rm -rf "${pkgdir}"/usr/share/info/*.png "${pkgdir}"/usr/share/info/*guile*
}

sha512sums=('e98f23a589042f879936c3f8b474535e695fb7dd68a9e81323668c013241f765c2d3af6c6a072ecf867acc1e551ec46e15bb842144d3a06bdd5d2f4fc3d828a7')
