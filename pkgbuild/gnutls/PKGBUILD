pkgname=gnutls
pkgver=3.5.17
pkgrel=1
pkgdesc="A library which provides a secure layer over a reliable transport layer"
arch=('x86_64')
url="http://www.gnutls.org/"
license=('GPL3' 'LGPL2.1')
#depends=()
source=("https://www.gnupg.org/ftp/gcrypt/gnutls/v${pkgver:0:3}/gnutls-${pkgver}.tar.xz")

build() {
	cd "${srcdir}/gnutls-${pkgver}"

	./configure --prefix=/usr \
		--enable-local-libopts \
		--disable-guile

	make
}

package() {
	cd "${srcdir}/gnutls-${pkgver}"

	make install DESTDIR="${pkgdir}"

	make -C doc/reference install-data-local DESTDIR="${pkgdir}"

	rm -rf "${pkgdir}"/usr/share/info/*.png "${pkgdir}"/usr/share/info/*guile*
}

sha512sums=('0cb3405dee64737e314bfa9eed6afa57a8951190fe576122aecaf587c07ec1a5df1ac4819078bff0d292271639019c76d9a708027b502e3f55e2cf6576555ea6')
