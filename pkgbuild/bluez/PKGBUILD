pkgname=bluez
pkgver=5.45
pkgrel=1
pkgdesc="Linux Bluetooth Protocol Stack"
arch=('x86_64')
url="http://www.bluez.org/"
arch=('i686' 'x86_64')
#depends=()
source=("https://www.kernel.org/pub/linux/bluetooth/bluez-${pkgver}.tar.xz")

build() {
	cd "${srcdir}/bluez-${pkgver}"

	./configure --prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--enable-experimental \
		--enable-library

	make
}

package() {
	cd "${srcdir}/bluez-${pkgver}"

	make install DESTDIR="${pkgdir}"

	install -v -dm755 "${pkgdir}/etc/bluetooth" "${pkgdir}/usr/lib/systemd/user"

	install -v -Dm644 src/main.conf "${pkgdir}/etc/bluetooth/main.conf"

	ln -sfv /usr/lib/systemd/user/obex.service "${pkgdir}/usr/lib/systemd/user/dbus-org.bluez.obex.service"
}

sha512sums=('ecefb2cddb5b40ac4b5650a849e5635d84ec3f6819849ca97af5a5d1eb220f0fe5734c81340c00c41e0a77613e19c35a9191a00094a2e4d9db6d597ec590010d')
