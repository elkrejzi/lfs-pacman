pkgname=gnome-control-center
pkgver=3.28.2
pkgrel=1
pkgdesc="GNOME's main interface to configure various aspects of the desktop"
arch=('x86_64')
url="https://git.gnome.org/browse/gnome-control-center/"
license=('GPL2')
#depends=()
source=("https://download.gnome.org/sources/gnome-control-center/${pkgver:0:4}/gnome-control-center-${pkgver}.tar.xz")

prepare() {
	install -v -dm755 "${srcdir}/build"
}

build() {
	cd "${srcdir}/build"

	meson --prefix /usr \
		--sysconfdir /etc \
		--localstatedir /var \
		--buildtype=plain \
		-Ddocumentation=true \
		"${srcdir}/gnome-control-center-${pkgver}"

	ninja ${MAKEFLAGS}
}

package() {
	cd "${srcdir}/build"

	DESTDIR="${pkgdir}" ninja install

	chown 27:root "${pkgdir}/usr/share/polkit-1/rules.d"
	chmod 700 "${pkgdir}/usr/share/polkit-1/rules.d"
}

sha512sums=('3e1b54cf1a603ee7c47d8b0f1fb025405c8d133c9e0dbe0eaf2f287eff82109e95581c79f131d227422d682b7049ccb6e6297dba1ff2276cff5f715308195d7f')
