pkgname=ruby
pkgver=2.4.2
pkgrel=1
pkgdesc="An object-oriented language for quick and easy programming"
arch=('x86_64')
url="http://www.ruby-lang.org/en/"
license=('BSD' 'custom')
#depends=()
options=(staticlibs emptydirs)
source=("http://cache.ruby-lang.org/pub/ruby/${pkgver:0:3}/ruby-${pkgver}.tar.xz")

build() {
	cd "${srcdir}/ruby-${pkgver}"

	./configure --prefix=/usr \
		--enable-shared \
		--disable-install-capi

	make
}

package() {
	cd "${srcdir}/ruby-${pkgver}"

	make install DESTDIR="${pkgdir}"
}

sha512sums=('c1d42272fb0d94b693452e703b0ea4942bf59cbd4b08ba83bf039f54be97ebc88511632413da0164970b4cf97bc302bccb88aab48edfa8fa147498e7ee741595')
