pkgname=totem-pl-parser
pkgver=3.25.90
pkgrel=1
pkgdesc="Simple GObject-based library to parse and save a host of playlist formats"
arch=('x86_64')
url="https://git.gnome.org/browse/totem-pl-parser/"
license=('LGPL')
#depends=()
source=("https://download.gnome.org/sources/totem-pl-parser/${pkgver:0:4}/totem-pl-parser-${pkgver}.tar.xz")

prepare() {
	install -v -dm755 "${srcdir}/build"
}

build() {
	cd "${srcdir}/build"

	meson --prefix /usr \
		--sysconfdir /etc \
		--localstatedir /var \
		--buildtype=plain \
		-Denable-gtk-doc=true \
		"${srcdir}/totem-pl-parser-${pkgver}"

	ninja ${MAKEFLAGS}
}

package() {
	cd "${srcdir}/build"

	DESTDIR="${pkgdir}" ninja install
}

sha512sums=('8b2d0f1f0b6caedd450e3830677cecc9fd1325391eeb15280fbc877a6b809052b9520db934edcb7ad910ca98b41cf8c87d6a3e5e0b62609da9dc2e5f90fafecf')
